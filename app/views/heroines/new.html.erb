<%= form_for (@heroine) do |f| %>
<%= f.label :name %><br>
<%= f.text_field :name %><br><br>

<%= f.label :super_name %><br>
<%= f.text_field :super_name %><br><br>

<%= f.label "Power:" %><br>
<%= f.collection_select(:power_ids, Power.all, :id, :name) %><br><br>

<%= f.submit %>
<% end %>