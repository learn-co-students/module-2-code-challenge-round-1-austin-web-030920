<h2>All Superpowers</h2>

<ul>
  <% @powers.each do |power| %>
    <li>
      <h3><%= link_to power.name, power_path(power) %></h3>
      <br>
      Description: <%= power.description %>
      Number of Heroines With Power:<%= power.heroines.count %>
    </li>
    <br>
  <% end %>
</ul>
